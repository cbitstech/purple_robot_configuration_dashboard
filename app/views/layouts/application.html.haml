!!!
%html
  %head
    %title PurpleConfiguration
    = stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true
    = javascript_include_tag "application", "data-turbolinks-track" => true
    = csrf_meta_tags
  %body
    = nav_bar :static => :top, :brand => "Purple Configurator" do
      = menu_group do
        = menu_item "Home", root_path
        = menu_item "Configurations", phone_configurations_path
      - if user_signed_in?
        = link_to 'Sign out', destroy_user_session_path, :method => :delete, :class => "btn btn-danger  pull-right"
    .container#top
      = yield
    .footer
